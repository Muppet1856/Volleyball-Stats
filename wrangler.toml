name = "volleyball-stats"
main = "src/index.js"
compatibility_date = "2024-04-03"

# Environment-specific overrides (optional, e.g., for production)
[env.production]
name = "volleyball-stats-prod"

# D1 binding will be appended by the workflow here if not present
